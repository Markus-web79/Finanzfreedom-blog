name: 🚀 Deploy Blog to Vercel

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v3

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📥 Install dependencies
        run: npm install

      # >>> Automatische Reparatur & Prüfung aller Markdown-Dateien <<<
      - name: 🧹 Fix Markdown Articles
        run: |
          npm run fix-content
          npm run fix-articles
          npm run fix-slugs
          npm run fix-umlaute

      - name: 🧩 Validate Markdown
        run: node scripts/checkMarkdown.js || true

      # >>> Qualitätssicherung vor dem Build <<<
      - name: 🧠 Check Content
        run: npm run lint || true

      # >>> Website bauen und exportieren <<<
      - name: 🏗️ Build
        run: npm run build

      - name: 📦 Export static site
        run: npm run export

      # >>> Deployment zu Vercel <<<
      - name: ▲ Deploy to Vercel
        run: npx vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
