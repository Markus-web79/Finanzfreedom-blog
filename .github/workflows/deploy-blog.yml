name: 🚀 Build & Deploy FinanzFreedom

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Generate and build
        run: |
          echo "📄 Generiere Artikel..."
          node scripts/generateArticles.js "ETF-Sparplan für Einsteiger"
          echo "🚀 Baue statische Next.js Seite..."
          npx next build

      - name: 🔄 Sitemap generieren
        run: node scripts/generateSitemap.js

      - name: Deploy to Vercel
        run: npx vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
