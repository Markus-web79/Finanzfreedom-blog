name: 🚀 Build and Deploy Blog

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 📥 Install dependencies
        run: npm install

- name: 🧠 Generiere Artikel
  run: |
    echo "🧠 Generiere Beispielartikel..."
    node scripts/generateArticle.js "ETF-Sparplan für Einsteiger" etfs

      - name: 🏗️ Baue statische Next.js Seite
        run: |
          echo "🏗️ Starte Next.js Build..."
          npx next build

      - name: 🗺️ Sitemap generieren
        run: |
          echo "🗺️ Erstelle Sitemap..."
          node scripts/generateSitemap.js

      - name: 🚀 Deploy to Vercel
        run: npx vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
