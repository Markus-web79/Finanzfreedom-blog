name: Check Content

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  spellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - run: npm ci || npm install

      - name: Run Spellcheck
        run: npx cspell "content/**/*.md" || true

      - name: Auto-Learn Unknown Words
        run: |
          npx cspell "content/**/*.md" --unique --words-only > new-words.txt
          grep -v -f cspell.json new-words.txt > filtered.txt || true
          if [ -s filtered.txt ]; then
            echo "Neue Wörter gefunden – Wörterbuch wird erweitert..."
            node scripts/addWords.js filtered.txt
            git config --global user.name "github-actions[bot]"
            git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add cspell.json
            git commit -m "📚 Wörterbuch automatisch erweitert"
            git push
          else
            echo "Keine neuen Wörter gefunden ✅"
          fi
