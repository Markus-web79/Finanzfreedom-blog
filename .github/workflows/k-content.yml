name: Check Content

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  content-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci || npm install

      - name: Run spell check
        run: npx cspell "content/**/*.md" --unique --words-only > new-words.txt

      - name: Add new words to dictionary
        run: |
          node scripts/addWords.js new-words.txt
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dictionary.json || echo "Nichts zu committen"
          git commit -m "🔤 Wörterbuch automatisch erweitert" || echo "Keine Änderungen"
          git push
